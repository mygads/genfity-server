# ===========================================
# Infrastructure Configuration (Root Level)
# ===========================================

# Database Configuration
DB_USER=genfity_user
DB_PASSWORD=genfity_password
DB_NAME=genfity_db
POSTGRES_MULTIPLE_DATABASES=wuzapi_db,event_api_db

# Timezone
TZ=Asia/Jakarta

# ===========================================
# SSL & Domain Configuration (Production Only)
# ===========================================

# Your email for Let's Encrypt SSL certificates
LETSENCRYPT_EMAIL=genfity@gmail.com

# ===========================================
# Services Domain Mapping & Reverse Proxy
# ===========================================
# Format: SERVICE_DOMAIN, SERVICE_PORT, SERVICE_CONTAINER
# When adding new service, just add these 3 variables and nginx config will be auto-generated

# WuzAPI Service (Port 8080 -> https://wa.genfity.com)
WUZAPI_DOMAIN=wa.genfity.com
WUZAPI_PORT=8080
WUZAPI_CONTAINER=genfity-wa

# Event API Service (Port 8070 -> https://webhook.genfity.com)
EVENTAPI_DOMAIN=webhook.genfity.com
EVENTAPI_PORT=8070
EVENTAPI_CONTAINER=genfity-chat-ai

# ===========================================
# Development Access Information
# ===========================================
# In development mode (without reverse proxy), services are accessible at:
# - WuzAPI: http://localhost:8080
# - Event API: http://localhost:8070
# - PostgreSQL: localhost:5432
#
# In production mode (with reverse proxy), services are accessible at:
# - WuzAPI: https://wa.genfity.com
# - Event API: https://webhook.genfity.com
#
# To add new service:
# 1. Add NEWSERVICE_DOMAIN, NEWSERVICE_PORT, NEWSERVICE_CONTAINER above
# 2. Nginx config will be auto-generated
# 3. Run: .\deploy.ps1 prod -GenerateConfig
